[project]
name = "data"
version = "0.1.0"
description = "Streamlit app for cleaning and converting files"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "streamlit",
    "pandas",
    "openpyxl",
    "xlsxwriter"
]

[tool.poetry]
package-mode = false
